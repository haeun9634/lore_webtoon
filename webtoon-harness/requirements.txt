requests>=2.31
PyYAML>=6.0
# 컷을 한 편으로 이어 붙이고(episode.py), 스티커·말풍선을 얹고(strip.py),
# 랜딩이 올라온 사진과 내려보낼 그림을 줄이는 데 쓴다. 없으면 1화 합치기가
# StitchError 로 멈추므로 선택 항목이 아니다 — 지금까지 안 적혀 있던 것이
# 빠뜨린 것이다.
Pillow>=10.0
